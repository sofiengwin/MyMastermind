class Player
    def initialize(user_name, user_level)
      @user_name = user_name
      @user_level = user_level
    end

    def name
      @user_name
    end

    def user_level
      @user_level
    end
end
